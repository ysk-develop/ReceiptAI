/**
 * App / cache version.
 * Bump this on every release so phones can confirm GitHub Pages deploy finished.
 * Also update the matching APP_VERSION in service-worker.js and the fallback text in index.html.
 */
export const APP_VERSION = '26';
