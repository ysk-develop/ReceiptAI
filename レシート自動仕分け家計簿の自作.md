> From: https://gemini.google.com/app/e4d7b0e2c9767430?hl=ja

# you asked

message time: 2026-09-13 20:14:32

家計管理ソフトを作りたいです。
例えば、レシートを複数枚無造作に並べた写真を撮影し、それをaiに画像判定してもらい、自動的に内容を仕分けてExcel（またはhtmlやPythonなどの自作アプリ内のsqliteなどのdb）に項目？種類？別に保存する。保存したデータはグラフにしたりなど一般的な家計管理ソフトの機能のようなもので確認や集計できる、みたいな仕組みは自作可能ですか？
撮影した写真を自作アプリに送る方法がキモになると思うのですが、サーバーがあるわけでもなく常にpcで待ち受けるのは厳しいので、例えば特定の専用アドレス宛にメールすると、pcアプリを起動させた時にメールボックスを読んで添付ファイルを自動取得してaiに掛けるとか？
他に使ってるメールで併用するなら、特定の件名や特定の本文からアプリ側で判定して取得するようにしなきゃだけど、スマホとかから送る際にいちいち特定の文言を入力するのは無理があるので、何かしら仕組み（自作アプリやhtmlのショートカットなど）が必要になるので、できれば専用メアドを用意するようにした方が良さそう。他には思いつかない。お金はかけたくないので無料でらできる範囲で。

---

# gemini response

結論から申し上げますと、**ご希望の仕組みは完全無料で自作可能**です。

全体の構成要素（画像送信・メール処理・AI判定・DB保存・GUI/グラフ表示）は、Pythonと無料のクラウド・標準機能を組み合わせることで、サーバー代やAPI費用を一切かけずに構築できます。

最も課題となる「スマホから写真をどう送るか」についても、入力の手間をゼロにするスマートな解決策があります。全体設計から最適な画像送信方法、おすすめの技術構成までを整理しました。

---

### 1. スマホからPCアプリへ写真を送る最適な方法

ご提案の「専用メールアドレスへの送信」は非常に現実的な選択肢ですが、送信のたびにアドレスを選んで写真を添付する手間を省くため、**スマホの自動化機能**を組み合わせるのがおすすめです。

#### A. iPhoneの「ショートカット」を使う（最もおすすめ・完全手軽）
iOS標準の「ショートカット」アプリを使用すると、共有シートからワンタップで送信を自動化できます。
* **仕組み**: 撮影したレシート写真を選び、「レシート送信」という自作ショートカットをタップするだけ。
* **動作**: 裏で特定の専用メールアドレス（例: 無料のGmail等）宛てに、件名や本文を自動設定して写真を即座にメール送信します。
* **メリット**: 件名や本文を毎回打ち込む必要がなく、数秒で送受信フローが完了します。（※Androidの場合は「Tasker」や「Automate」等の無料・低価格アプリで同様の仕組みが作れます）

#### B. クラウドストレージの自動同期を使う（メールすら使わない方法）
Google ドライブや OneDrive の無料枠（5GB〜15GB）を利用する方法です。
* **仕組み**: スマホで撮影した写真を Google ドライブ等の専用フォルダに保存（またはスマホアプリからアップロード）する。
* **動作**: PC側でその同期フォルダを監視しておき、新しい画像が入ってきたら自動で検出してAI処理を行います。
* **メリット**: メールサーバーの読み込み（IMAP処理）を書く必要がなく、ファイル入出力の処理だけで済むためPC側のプログラム記述が最もシンプルになります。

---

### 2. 全体のシステム構成案（完全無料）

完全無料で構築するための最適な技術スタックです。

```
[スマホ] --(iOSショートカット or ドライブ同期)--> [受信 / 同期フォルダ]
                                                        │
                                                        ▼
[PCアプリ (Python)] ───(Gemini API: 無料枠)───> [AI解析 (テキスト・カテゴリ化)]
        │                                               │
        ▼                                               ▼
 [SQLite DB保存] <──────────────────────────────────────┘
        │
        ▼
[GUI画面 / グラフ表示 (CustomTkinter / Matplotlib)]
```

#### ① 写真の受取 (Python `imaplib` または `pathlib`)
* **メールの場合**: Pythonの標準ライブラリ（`imaplib`, `email`）で、指定したGmail等の未読メールを定期取得し、添付画像をローカルに保存してメールを既読（または削除）にします。
* **ドライブ同期の場合**: `watchdog` ライブラリなどで特定フォルダへの画像追加を検知します。

#### ② AI画像判定 (Google Gemini API - 無料枠)
複数枚のレシートが乱雑に並んだ画像から「日付」「店舗名」「品目」「金額」「カテゴリ（食費・日用品等）」を自動抽出するには、マルチモーダルAIが最適です。
* **Google Gemini API (`gemini-2.5-flash` など)**:
  * 画像認識能力が高く、雑に置かれた複数レシートの解析にも優れています。
  * **個人利用の範囲であれば無料利用枠（Free Tier）で十分に収まります**。
  * レスポンス形式を `JSON` に指定することで、判定結果を直接プログラムで扱いやすいデータ構造（`{"date": "2026-09-13", "items": [...]}`）で取得可能です。

#### ③ データベース保存 (SQLite)
* Python標準の `sqlite3` を使用します。
* テーブル構造例：
  * `receipts`（ID, 撮影日時, 合計金額, 店舗名）
  * `details`（ID, レシートID, 品目名, 金額, カテゴリ）

#### ④ アプリ画面・集計・グラフ (CustomTkinter / Matplotlib)
* **GUI構成**: Pythonの `CustomTkinter`（モダンなデスクトップ画面を簡単に作れるライブラリ）を採用すると、見栄えの良い家計簿アプリを作成できます。
* **グラフ表示**: `Matplotlib` または `Seaborn` をGUI内に埋め込み、月別カテゴリ比率のパイチャートや推移棒グラフを描画します。

---

### 3. 実装の流れ（自作ステップ）

1. **Gemini APIキーの取得**: Google AI Studioから無料でAPIキーを発行します。
2. **AI解析プロンプトのテスト**: Pythonスクリプトを作成し、テスト画像を渡して「複数レシートの項目・金額・カテゴリをJSONで返して」という指示が正しく動作するか検証します。
3. **受信連携の実装**: メール受信（またはフォルダ監視）から画像を取り出し、手順2の処理へ引き渡す自動化スクリプトを書きます。
4. **DB設計と保存**: 解析結果のJSONをSQLiteに入力する処理を書きます。
5. **UI・グラフ作成**: SQLiteからデータを取得して集計し、画面にグラフやリストを表示する機能を実装します。

PCを常時稼働させる必要はなく、「アプリを起動したタイミングで未処理のメール/画像をまとめて一括処理する」仕様にすれば、負荷もかからずスムーズに運用可能です。

<FollowUp label="この構成でPythonのAI解析スクリプト（プロンプト例含む）を作成してみますか？" query="レシート写真をGemini APIに渡してJSONで品目やカテゴリを出力させるPythonのサンプルコードを書いてください。"/>

---

# you asked

message time: 2026-09-13 20:23:06

Python側では、解析したものをまずは一覧で表示し、修正なども可能（誤読対策）としたいです。また、例えばレシートが無い場合なども、手入力追加できるような仕組みも必要です。
あるいは、スマホ側でも、画像の代わりにテキスト入力した本文も送信可能とし、本文より解析可能とする機能も必要です。品目や種類などから選択式にすると汎用性が低くなるため、「◯◯店、ティッシュ、235円」といったメモ程度の本文からでもaiが解析してくれるタイプの方が良いです。

---

# gemini response

誤読の修正機能や手入力、さらにテキストメモからのAI自動解析など、実用的な家計簿ソフトに必要な機能がすべて整理されていて素晴らしいアイデアです。

ご希望の機能（確認・修正ビュー、手入力追加、テキストメモのAI解析）も含めた**Pythonアプリ全体の設計と実装の構成案**をご提案します。

---

### 1. 全体の処理フロー設計

```
【入力ソース】
 ├── ① レシート写真（メール添付 or 同期フォルダ）
 └── ② テキストメモ（メール本文「◯◯店、ティッシュ、235円」）
         │
         ▼
【AI解析処理 (Gemini API / 無料枠)】
 ├── 画像 ────> 画像認識 ───┐
 └── テキスト ─> 文脈解析 ───┴─> 一時データ（JSON）
                                      │
                                      ▼
【データ確認・修正・手入力画面 (CustomTkinter GUI)】
 ├── AI解析結果のテーブル表示
 ├── セルを直接編集（誤読修正）
 ├── 手入力行の追加ボタン
 └── 「確定・保存」ボタン ───> 【SQLite DBへ本保存】
```

---

### 2. キーポイントとなる機能の実装アプローチ

#### A. AI解析（画像 ＆ テキストメモの両対応）
Gemini APIは画像だけでなくテキストの文脈解釈も非常に強力です。プロンプトを工夫することで、入力が「画像」でも「雑なテキストメモ」でも、**同じ共通のJSONフォーマット**で結果を出力させることができます。

* **プロンプトの工夫**:
  * 画像の場合: 「レシート画像から店舗名、日付、品目、金額、カテゴリ（食費・日用品等）を抽出し、JSONで返してください」
  * テキストの場合: 「『ドラッグストア、ティッシュ 235円、お茶 100円』のような雑多なメモから、店舗名、品目、金額、カテゴリを推測してJSONで返してください」

どちらを入力にしても同じ形式のデータ構造として受け取るため、その後のGUI処理を共通化できます。

#### B. 一覧表示・編集・手入力画面（GUI）
Pythonの `CustomTkinter` と `CTkTable`（または標準の `tkinter.ttk.Treeview`）を使用すると、以下のような確認・修正画面が作れます。

1. **未確定データの一覧表示**: AI解析が終わると、まず「仮保存テーブル」に一覧表示されます。
2. **誤読のインライン修正**: 金額や品目名を画面上のセルでダブルクリックして修正できるようにします。
3. **新規手入力追加**: 「＋ 行を追加」ボタンを用意し、レシートがない場合でも空の行を挿入して手動入力できるようにします。
4. **カテゴリの補正**: AIが自動分類したカテゴリ（例: 日用品、食費など）が違っていれば、ドロップダウンやテキスト入力で修正可能です。
5. **DB保存**: 「保存」ボタンを押したタイミングで、画面上の確定データのみがSQLiteデータベースへ正式にコミットされます。

#### C. メールの受取処理（画像 or 本文の自動判別）
Pythonのメール取得処理（`imaplib`）で以下のように自動分岐させます。

```python
# 疑似コード例
if email.has_attachments():
    # 添付画像がある場合 -> 画像解析APIへ
    image = get_attachment()
    result = analyze_receipt_image(image)
elif email.body_text:
    # 添付がなく本文テキストがある場合 -> テキスト解析APIへ
    text = email.body_text
    result = analyze_receipt_text(text)
```

---

### 3. 今後の開発ステップ案

この仕組みを作成する場合、以下のような手順で段階的に作っていくのがおすすめです。

1. **ステップ1: Gemini APIで「画像」と「テキストメモ」をJSON化する試作**
   * まずは画面なしで、PythonスクリプトからGemini APIを呼び出し、画像と雑なテキストから正しくJSONが得られるか確認します。
2. **ステップ2: 確認・修正・手入力ができるGUI画面の作成**
   * `CustomTkinter` を使って、テーブル表示・編集・行追加・SQLite保存ができる画面を作ります。
3. **ステップ3: メール受信（またはフォルダ監視）の自動化**
   * メールから未読メッセージの添付ファイル/本文を取り出す処理を組み込みます。
4. **ステップ4: グラフ集計機能の追加**
   * SQLiteから月別の支出データを読み込み、`Matplotlib` でカテゴリ別パイチャートや月次推移グラフを表示するタブを作成します。

---

まず最初に、**「画像」と「テキストメモ」の両方をAIで解析して修正可能なデータ形式にするPythonコードのサンプル**から作成してみますか？

<FollowUp label="AI解析スクリプトとGUI確認画面（修正・手入力機能つき）のベースコードを作成しますか？" query="Gemini APIで画像とテキストメモを解析し、CustomTkinterで修正・手入力追加ができるPythonのベースプログラムを作成してください。"/>

---

# you asked

message time: 2026-09-13 20:25:40

Ai解析は、スマホ側が良いと思いますか？pc側が良いと思いますか？それとも両方に実装する事も可能ですか？添付したファイルはスマホ側で判定できるようなプログラムです。

---

# gemini response

スマホ側とPC側のどちらでAI解析を行うか、結論から言うと**「まずはスマホ（HTML/Web）側で直接解析を行う」構成をメインとしつつ、状況に応じて両方対応できるようにしておく**のが最も使い勝手が良くなります。

添付していただいた「EV充電 最安判定」のHTMLプログラムは、非常にスマートな設計になっています。
このコードでは、バックエンド（サーバー）を構築せず、ブラウザ（JavaScript）から直接 **Gemini API Key** を使って画像をAPIへ送信し、レスポンスとしてJSONを受け取る仕組みを採用しています。

家計簿ソフトにおいても、この設計思想をそのまま応用・発展させることができます。

---

### 1. 各アプローチの比較（メリット・デメリット）

| 比較項目 | ① スマホ側（HTML/Web）で判定 | ② PC側（Python）で判定 | ③ 両方（ハイブリッド） |
| :--- | :--- | :--- | :--- |
| **処理のイメージ** | 撮影したその場でWebアプリから直接Gemini APIを叩く | メールやフォルダ同期でPCに送り、Pythonで一括処理 | スマホで即時判定し、結果のデータ（JSON）だけPCに送る |
| **手間の少なさ** | ⭕ 撮影後、数秒でその場でAIの解析結果を確認できる | ❌ メール送信やファイル同期の手間が1ステップ挟まる | ⭕ スマホで完結。PCアプリを開くと自動的に同期される |
| **誤読の修正** | ⭕ スマホ画面上でその場で直せる | ⭕ PCの大きな画面でゆっくり直せる | ⭕ スマホでもPCでもどちらでも修正可能 |
| **APIキーの扱い** | スマホのブラウザ（`localStorage`）に保持 | PCのスクリプト内に保持 | それぞれの端末で保持（または共有） |

---

### 2. なぜ「スマホ（HTML）側での解析」がおすすめか？

家計簿のレシート撮影において、最大のネックは**「後からAIの誤認識を直す作業」**です。

PC側だけで解析する場合、夜にPCを開いて一括処理した際、「あ、この金額違うけど、昼買ったレシート捨てちゃったから正解がわからない…」という事態が起こり得ます。

添付のHTMLのように**スマホ側で即時解析**できれば：
1. レシートを撮る
2. その場でAIの解析結果（店舗名・金額・品目）が画面に出る
3. 誤読があればレシートを見ながらその場で手修正
4. 「保存」を押す

という、**記憶が鮮明で現物もある状態**で完結できます。

---

### 3. 「両方に実装する」おすすめのハイブリッド構成

「基本はスマホで解析するけれど、大量のレシートを一度に処理したい時やPCで過去データをグラフ管理したい」という要望を満たすため、**「スマホ＝入力・解析・手修正用」「PC＝蓄積・集計・グラフ用」** という役割分担が最適です。

#### 構成イメージ

1. **スマホ（添付のHTMLを発展させたWebアプリ）**
   * カメラ起動 / テキストメモ入力
   * スマホ側のJavaScriptからGemini APIを直接呼び出して解析
   * 解析結果を画面上で確認・手修正・新規手入力
   * 確定したデータを **Googleスプレッドシート（無料API）** または **JSONファイル（Googleドライブ等）** へ保存

2. **PC（Pythonアプリ）**
   * PCアプリ起動時に、Googleスプレッドシートや共有フォルダからデータ（すでに判定・修正済みの綺麗なデータ）を読み込む
   * SQLiteへの流し込み、および `Matplotlib` / `CustomTkinter` によるグラフ表示・詳細集計・CSV出力を行う
   * （オプション）PC側に写真を直接ドラッグ＆ドロップした時用に、Python側にもGemini API解析機能を載せておく

---

### 4. 添付プログラムからの拡張ポイント

添付いただいた `EV充電_最安判定.html` の設計（`localStorage` にAPIキーを保存し、`fetch` でGeminiの `generateContent` を叩き、`responseSchema` でJSON構造を指定する手法）は、家計簿ソフトにもそのまま移殖できます。

家計簿用にする場合、Geminiへ渡す `responseSchema` を以下のように変更するだけで対応可能です。

```javascript
// レシート画像・テキストメモ共通のJSONスキーマ定義例
generationConfig: {
  responseMimeType: "application/json",
  responseSchema: {
    type: "OBJECT",
    properties: {
      shop_name: { type: "STRING" },  // 店舗名
      date: { type: "STRING" },       // 日付 (YYYY-MM-DD)
      total_amount: { type: "NUMBER" },// 合計金額
      items: {
        type: "ARRAY",
        items: {
          type: "OBJECT",
          properties: {
            name: { type: "STRING" },     // 品目名
            price: { type: "NUMBER" },    // 単価・金額
            category: { type: "STRING" } // カテゴリ（食費、日用品等）
          },
          required: ["name", "price", "category"]
        }
      }
    },
    required: ["shop_name", "date", "total_amount", "items"]
  }
}
```

### まとめ

* **AI解析の場所**: 撮影したその場で修正できる**スマホ（HTML）側**での解析をメインにするのが最も便利です。
* **データの流れ**: 「スマホで解析＆手修正」→「データだけをクラウド/メール等で転送」→「PCでSQLite保存＆グラフ表示」とするのが、メールで重い画像を送る必要もなくスムーズです。
* **両方への実装**: スマホ側をメインにしつつ、PCアプリ（Python）側にも同じGemini解析ロジックを入れておけば、万が一PCに画像を直接読み込ませたい場合にも柔軟に対応できます。

---

# you asked

message time: 2026-09-13 20:29:29

このシステムを構築するにあたり、メールとGoogleドライブでは、どちらが良いと思いますか？
iPhone上のhtmlから「送信」とボタン押したら解析したjson結果をメールするなりdriveに保存するなりを自動的にできますか？

---

# gemini response

結論から申し上げますと、iPhone上のHTML（Webアプリ）からボタンを押して自動連携させる場合、**「Googleドライブ（Google Forms / GAS経由）」の方が圧倒的におすすめ**です。

それぞれの特徴と、iPhone上のHTMLから「送信」ボタン一発で自動送信できる仕組みについて解説します。

---

### 1. メールとGoogleドライブの比較

| 比較項目 | ① メール送信 | ② Googleドライブ（GAS連携） |
| :--- | :--- | :--- |
| **ボタン一発での自動送信** | ❌ **不可**（メールアプリが起動し、送信ボタンを手動で押す必要がある） | ⭕ **完全自動**（画面遷移なしで裏で送信完了） |
| **PC側での取得のしやすさ** | 🔺 IMAP受信処理（メールボックスの巡回・パース）が必要 | ⭕ フォルダ同期（Google Drive for Desktop）で自動ローカル保存 |
| **エラーの発生しやすさ** | 🔺 送信漏れやスパム判定のリスクがある | ⭕ 安定してHTTP通信（`fetch`）で届く |
| **通信量・コスト** | ⭕ 無料 | ⭕ 無料（GAS・ドライブともに無料枠で余裕） |

#### メール送信の最大のネック（ブラウザのセキュリティ制限）
ブラウザ（HTML/JavaScript）からメールを送る場合、セキュリティ上の制約により**JavaScript単体でメールを無言で送信することはできません**。`mailto:` リンクを使う方法になりますが、これだと「標準メールアプリが開き、ユーザーが送信ボタンを押す」という2重の手間が発生してしまいます。

---

### 2. Googleドライブ連携なら「送信」ボタン一発で自動化可能！

iPhone上のHTMLから「保存」や「送信」ボタンを押した際、画面を切り替えずに裏でJSONデータをGoogleドライブへ自動保存する仕組みは**完全自動で構築可能**です。

これを実現するために、**Google Apps Script (GAS)** を無料の「受け口API」として利用します。

#### 自動連携の仕組み

```
[iPhone: HTML画面] 
  │ 「保存」ボタンをタップ
  ▼
[JavaScript (fetch通信)] ───(JSONデータ送信)───> [Google Apps Script (GAS)]
                                                        │
                                                        ▼
                                           [Googleドライブの指定フォルダ]
                                             └─ 2026-09-13_183020.json 保存
                                                        │
                                                        ▼ (自動同期)
                                           [PCのローカルフォルダ (SQLite入力用)]
```

#### 実装の流れ

1. **GAS（Google Apps Script）で受け口を作成（数行のコード）**
   Googleドライブ側で短いスクリプトを作成し、Web API（ウェブアプリ）として公開します。データを受け取ったら指定フォルダに `.json` ファイルとして保存する処理です。
   ```javascript
   // GAS側のコード例
   function doPost(e) {
     const data = JSON.parse(e.postData.contents);
     const folder = DriveApp.getFolderById("ここにGoogleドライブのフォルダID");
     const fileName = `receipt_${new Date().getTime()}.json`;
     folder.createFile(fileName, JSON.stringify(data, null, 2), MimeType.PLAIN_TEXT);
     return ContentService.createTextOutput(JSON.stringify({status: "ok"}));
   }
   ```

2. **iPhoneのHTML（JavaScript）から送信**
   添付していただいたHTMLの `showResult` などの後に、以下のような `fetch` 処理を入れるだけで、**画面を動かさずにボタン一発で送信完了**します。
   ```javascript
   // HTML側の送信ボタン処理例
   async function sendToDrive(jsonData) {
     const gasUrl = "https://script.google.com/macros/s/XXX.../exec"; // GASのURL
     await fetch(gasUrl, {
       method: "POST",
       body: JSON.stringify(jsonData)
     });
     alert("Googleドライブへ保存しました！");
   }
   ```

3. **PC側での受け取り**
   PCに公式の **「Google Drive for Desktop（無料）」** を入れておくと、クラウド上の指定フォルダに保存されたJSONファイルが、PCの特定のローカルフォルダへ数秒で自動同期されます。
   PCのPythonアプリ側は、そのローカルフォルダに新しい `.json` が入ってきたらSQLiteに流し込むだけでOKです。

---

### まとめ

* **推奨:** **Googleドライブ（GAS経由）**
* **理由:** メールのように別アプリを挟む必要がなく、HTML上の「送信」ボタンを一度押すだけで、裏で通信して即座にドライブにJSONを蓄積できるためです。

この構成にすると、スマホアプリ側は「解析」→「修正」→「送信ボタン一発」で操作が完結し、ストレスなく運用できます。

---

# you asked

message time: 2026-09-18 20:50:37

まずはスマホアプリのソースコードを作成してください

---

# gemini response

スマホ用のHTML・JavaScriptコードを作成しました。

ご提示いただいた「EV充電 最安判定」のプログラム（`localStorage`を利用したAPIキー・マスタの保存、Gemini APIの`responseSchema`によるJSON抽出、iOSに配慮したレスポンシブデザイン）の設計思想を踏襲し、**家計簿用に最適化された単一ファイルのWebアプリ（HTML/JS）**として構築しています。

---

### スマホ用アプリ (`receipt_scanner.html`)

```html
<!doctype html>
<html lang="ja">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover">
<meta name="apple-mobile-web-app-capable" content="yes">
<meta name="apple-mobile-web-app-status-bar-style" content="default">
<meta name="theme-color" content="#111827">
<title>家計簿レシートスキャナー</title>
<style>
:root{--bg:#f5f7fb;--card:#fff;--text:#111827;--muted:#6b7280;--line:#dbe1ea;--accent:#2563eb;--good:#087f5b;--danger:#b91c1c}
*{box-sizing:border-box}
body{margin:0;background:var(--bg);color:var(--text);font-family:-apple-system,BlinkMacSystemFont,"SF Pro Text","Hiragino Sans","Yu Gothic",sans-serif}
main{max-width:760px;margin:auto;padding:16px 16px 48px}
h1{font-size:24px;margin:4px 0 8px}
h2{font-size:16px;margin:0 0 10px}
p{line-height:1.5;margin:4px 0}
.card{background:var(--card);border:1px solid var(--line);border-radius:16px;padding:16px;margin:12px 0;box-shadow:0 2px 8px rgba(0,0,0,.03)}
.sub{color:var(--muted);font-size:13px}
button,.filebtn{appearance:none;border:0;border-radius:10px;padding:10px 14px;background:var(--accent);color:#fff;font-size:15px;font-weight:700;cursor:pointer}
button.secondary{background:#e8edf5;color:#1f2937}
button.success{background:var(--good)}
button.danger{background:#fee2e2;color:var(--danger)}
button:disabled{opacity:.5;cursor:not-allowed}
input,select,textarea{width:100%;font:inherit;border:1px solid var(--line);border-radius:8px;padding:8px 10px;background:#fff}
input[type=file]{display:none}
.preview{display:none;width:100%;max-height:280px;object-fit:contain;border-radius:10px;border:1px solid var(--line);margin-top:10px;background:#111}
.actions{display:flex;gap:8px;flex-wrap:wrap;align-items:center}
.status{padding:10px;border-radius:8px;background:#f1f5f9;color:#334155;font-size:13px;white-space:pre-wrap;margin-top:10px}
.item-row{display:grid;grid-template-columns:1fr 100px 90px 36px;gap:6px;align-items:center;margin-bottom:8px}
.item-head{font-size:12px;color:var(--muted);font-weight:700;margin-bottom:4px}
@media(max-width:480px){
  .item-row{grid-template-columns:1fr 80px 75px 32px}
  .item-row input,.item-row select{padding:6px;font-size:13px}
}
.total-box{display:flex;justify-content:space-between;align-items:center;padding:12px;background:#ecfdf5;border:1px solid #a7f3d0;border-radius:10px;margin-top:12px;font-weight:700}
.total-price{font-size:20px;color:var(--good)}
.small{font-size:12px;color:var(--muted)}
details summary{cursor:pointer;font-weight:700;font-size:14px}
label{display:block;font-weight:700;font-size:13px;margin:8px 0 4px}
hr{border:0;border-top:1px solid var(--line);margin:12px 0}
</style>
</head>
<body>
<main>
  <h1>🧾 家計簿スキャナー</h1>
  <p class="sub">レシート写真またはテキストメモからAIが品目・金額を抽出します。</p>

  <!-- 入力エリア -->
  <section class="card">
    <h2>① 入力（写真 または テキスト）</h2>
    <div class="actions">
      <label class="filebtn" style="display:inline-block;margin:0">
        📷 レシート撮影/選択
        <input id="imageInput" type="file" accept="image/*">
      </label>
      <button id="clearBtn" class="secondary">クリア</button>
    </div>
    <img id="preview" class="preview" alt="選択した画像">
    
    <label for="textMemo" style="margin-top:12px">テキストメモ入力（画像がない場合）</label>
    <textarea id="textMemo" rows="2" placeholder="例：スーパーABC、牛乳 198円、食パン 120円"></textarea>
    
    <div class="actions" style="margin-top:10px">
      <button id="analyzeBtn" style="width:100%">AIで解析・抽出</button>
    </div>
    <div id="status" class="status">レシート写真を選択するか、メモを入力してください。</div>
  </section>

  <!-- 解析・編集結果エリア -->
  <section class="card" id="editorCard" style="display:none">
    <h2>② 内容確認・修正</h2>
    
    <div style="display:grid;grid-template-columns:1fr 1fr;gap:8px">
      <div>
        <label for="shopName">店舗名</label>
        <input id="shopName" placeholder="例: ○○スーパー">
      </div>
      <div>
        <label for="receiptDate">日付</label>
        <input id="receiptDate" type="date">
      </div>
    </div>

    <label>明細一覧</label>
    <div class="item-row item-head">
      <div>品目名</div><div>金額(円)</div><div>カテゴリ</div><div></div>
    </div>
    <div id="itemList"></div>

    <button id="addItemBtn" class="secondary" style="width:100%;margin-top:4px">+ 明細行を追加</button>

    <div class="total-box">
      <span>合計金額</span>
      <span id="totalPrice" class="total-price">0 円</span>
    </div>

    <div class="actions" style="margin-top:16px">
      <button id="sendBtn" class="success" style="width:100%;font-size:17px;padding:12px">☁ Googleドライブへ保存（送信）</button>
    </div>
  </section>

  <!-- 設定・料金マスターエリア -->
  <section class="card">
    <details id="settingsDetails">
      <summary>⚙️ 設定（APIキー・GAS URL）</summary>
      
      <label for="apiKey">Gemini API Key</label>
      <input id="apiKey" type="password" autocomplete="off" placeholder="AIzaSy...">
      
      <label for="gasUrl">GAS（Google Apps Script）Web App URL</label>
      <input id="gasUrl" placeholder="https://script.google.com/macros/s/.../exec">
      
      <label for="model">Gemini モデル</label>
      <input id="model" value="gemini-2.5-flash">

      <div class="actions" style="margin-top:12px">
        <button id="saveSettingsBtn">設定を保存</button>
      </div>
      <p class="small" style="margin-top:8px">※ 設定情報はこの端末のlocalStorageに保存されます。</p>
    </details>
  </section>
</main>

<script>
const CATEGORIES = ["食費", "日用品", "交通費", "交際費", "衣服・美容", "趣味・教養", "医療・健康", "水道・光熱費", "その他"];

const $ = id => document.getElementById(id);
let imageData = null;
let imageMime = "image/jpeg";

// 画面初期化
window.addEventListener("DOMContentLoaded", () => {
  const today = new Date().toISOString().split("T")[0];
  $("receiptDate").value = today;
  
  // 設定の読み込み
  const key = localStorage.getItem("gemini_api_key");
  const gas = localStorage.getItem("gas_script_url");
  if(key) $("apiKey").value = key;
  if(gas) $("gasUrl").value = gas;
  if(!key || !gas) $("settingsDetails").open = true;
});

// 設定保存
$("saveSettingsBtn").onclick = () => {
  const k = $("apiKey").value.trim();
  const g = $("gasUrl").value.trim();
  if(!k) return alert("Gemini API Keyを入力してください。");
  localStorage.setItem("gemini_api_key", k);
  localStorage.setItem("gas_script_url", g);
  alert("設定を保存しました。");
};

// 画像選択
$("imageInput").onchange = async e => {
  const file = e.target.files?.[0];
  if(!file) return;
  imageMime = file.type || "image/jpeg";
  const dataUrl = await fileToDataURL(file);
  imageData = dataUrl.split(",")[1];
  $("preview").src = dataUrl;
  $("preview").style.display = "block";
  $("status").textContent = `画像を読み込みました: ${file.name}`;
};

$("clearBtn").onclick = () => {
  imageData = null;
  $("imageInput").value = "";
  $("textMemo").value = "";
  $("preview").style.display = "none";
  $("editorCard").style.display = "none";
  $("status").textContent = "レシート写真を選択するか、メモを入力してください。";
};

function fileToDataURL(file){
  return new Promise((resolve, reject) => {
    const r = new FileReader();
    r.onload = () => resolve(r.result);
    r.onerror = reject;
    r.readAsDataURL(file);
  });
}

// 明細行のレンダリング
function renderItems(items = []){
  const container = $("itemList");
  container.innerHTML = "";
  if(items.length === 0) addItemRow();
  else items.forEach(item => addItemRow(item.name, item.price, item.category));
  calcTotal();
}

function addItemRow(name = "", price = "", category = "食費"){
  const div = document.createElement("div");
  div.className = "item-row";
  
  const optionsHtml = CATEGORIES.map(c => 
    `<option value="${c}" ${c === category ? "selected" : ""}>${c}</option>`
  ).join("");

  div.innerHTML = `
    <input class="i-name" value="${escapeHtml(name)}" placeholder="品目名">
    <input class="i-price" type="number" value="${price}" placeholder="金額">
    <select class="i-cat">${optionsHtml}</select>
    <button class="danger" style="padding:6px;font-size:12px" onclick="this.parentElement.remove();calcTotal();">✕</button>
  `;
  
  div.querySelector(".i-price").addEventListener("input", calcTotal);
  $("itemList").appendChild(div);
  calcTotal();
}

$("addItemBtn").onclick = () => addItemRow();

function calcTotal(){
  let total = 0;
  document.querySelectorAll(".i-price").forEach(el => {
    total += Number(el.value) || 0;
  });
  $("totalPrice").textContent = `${total.toLocaleString()} 円`;
}

function escapeHtml(s){
  return String(s || "").replace(/[&<>"']/g, c => ({ "&":"&amp;", "<":"&lt;", ">":"&gt;", '"':"&quot;", "'":"&#39;" }[c]));
}

// AI解析処理
$("analyzeBtn").onclick = async () => {
  const key = localStorage.getItem("gemini_api_key") || $("apiKey").value.trim();
  if(!key){ alert("設定からGemini API Keyを保存してください。"); $("settingsDetails").open = true; return; }

  const textMemo = $("textMemo").value.trim();
  if(!imageData && !textMemo){ alert("写真を選択するか、テキストメモを入力してください。"); return; }

  $("status").textContent = "Geminiで解析中...";
  $("analyzeBtn").disabled = true;

  const prompt = `
あなたは優秀な家計簿アシスタントです。
提供されたレシート画像またはテキストメモから、以下の情報を抽出してください。
- shop_name: 店舗名（不明な場合は"不明"）
- date: 日付（YYYY-MM-DD形式。不明なら今日の日付）
- items: 品目リスト（品目名 name, 金額 price, カテゴリ category）

カテゴリは必ず以下から選択してください：
[食費, 日用品, 交通費, 交際費, 衣服・美容, 趣味・教養, 医療・健康, 水道・光熱費, その他]

JSONのみを出力してください。
`;

  const parts = [];
  if(imageData){
    parts.push({ inline_data: { mime_type: imageMime, data: imageData } });
  }
  if(textMemo){
    parts.push({ text: `【入力メモ】\n${textMemo}` });
  }
  parts.push({ text: prompt });

  const body = {
    contents: [{ parts: parts }],
    generationConfig: {
      responseMimeType: "application/json",
      responseSchema: {
        type: "OBJECT",
        properties: {
          shop_name: { type: "STRING" },
          date: { type: "STRING" },
          items: {
            type: "ARRAY",
            items: {
              type: "OBJECT",
              properties: {
                name: { type: "STRING" },
                price: { type: "NUMBER" },
                category: { type: "STRING" }
              },
              required: ["name", "price", "category"]
            }
          }
        },
        required: ["shop_name", "date", "items"]
      }
    }
  };

  try {
    const model = $("model").value.trim() || "gemini-2.5-flash";
    const url = `https://generativelanguage.googleapis.com/v1beta/models/${encodeURIComponent(model)}:generateContent`;
    const resp = await fetch(url, {
      method: "POST",
      headers: { "x-goog-api-key": key, "Content-Type": "application/json" },
      body: JSON.stringify(body)
    });

    const raw = await resp.text();
    if(!resp.ok) throw new Error(`HTTP ${resp.status}\n${raw}`);
    
    const resData = JSON.parse(raw);
    const text = resData?.candidates?.[0]?.content?.parts?.map(p => p.text || "").join("") || "";
    const parsed = JSON.parse(text);

    // フォームに反映
    $("shopName").value = parsed.shop_name || "";
    if(parsed.date && parsed.date.match(/^\d{4}-\d{2}-\d{2}$/)) {
      $("receiptDate").value = parsed.date;
    }
    renderItems(parsed.items || []);
    
    $("editorCard").style.display = "block";
    $("status").textContent = "解析完了。内容を確認・修正してください。";
  } catch(err) {
    console.error(err);
    $("status").textContent = `解析エラー: ${err.message}`;
  } finally {
    $("analyzeBtn").disabled = false;
  }
};

// Googleドライブ（GAS）へJSON送信
$("sendBtn").onclick = async () => {
  const gasUrl = localStorage.getItem("gas_script_url") || $("gasUrl").value.trim();
  if(!gasUrl){ alert("設定からGASのWeb App URLを保存してください。"); $("settingsDetails").open = true; return; }

  // 画面の修正済みデータを集計
  const items = [];
  document.querySelectorAll("#itemList .item-row").forEach(row => {
    const name = row.querySelector(".i-name").value.trim();
    const price = Number(row.querySelector(".i-price").value) || 0;
    const category = row.querySelector(".i-cat").value;
    if(name || price > 0){
      items.push({ name, price, category });
    }
  });

  let totalAmount = 0;
  items.forEach(i => totalAmount += i.price);

  const payload = {
    timestamp: new Date().toISOString(),
    shop_name: $("shopName").value.trim() || "不明",
    date: $("receiptDate").value,
    total_amount: totalAmount,
    items: items
  };

  $("sendBtn").disabled = true;
  $("sendBtn").textContent = "送信中...";

  try {
    // GAS側へPOST送信
    await fetch(gasUrl, {
      method: "POST",
      mode: "no-cors", // GASのCORS制約回避
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify(payload)
    });

    alert("Googleドライブへ送信しました！");
    $("clearBtn").click();
  } catch(err) {
    console.error(err);
    alert(`送信エラー: ${err.message}`);
  } finally {
    $("sendBtn").disabled = false;
    $("sendBtn").textContent = "☁ Googleドライブへ保存（送信）";
  }
};
</script>
</body>
</html>
```

---

### アプリの特徴・使い方

1. **写真とテキストの両対応**
   * スマホから「レシート撮影」を選ぶか、「テキストメモ」に「○○店 牛乳200円」などと打ち込んで「AIで解析・抽出」ボタンを押すと、自動的に「店舗名」「日付」「品目・金額・カテゴリ」に分割されます。
2. **直感的な修正・追加**
   * 解析結果が画面（② 内容確認・修正）にグリッドで展開され、金額やカテゴリの誤読をタップしてその場で修正できます。
   * 「＋ 明細行を追加」ボタンで、レシートにない品目や手動入力も可能です。
3. **Googleドライブ（GAS）へボタン一発送信**
   * 「☁ Googleドライブへ保存」を押すと、修正済みのJSONデータがバックグラウンドで指定のGoogleドライブフォルダへ保存されます。

---

### 保存先のGoogle Apps Script（GAS）側のコード例

Googleドライブ側の準備として、無料のGoogle Apps Scriptで以下を作成し、「ウェブアプリ」としてデプロイ（全員にアクセス許可）しておきます。

```javascript
function doPost(e) {
  try {
    const data = JSON.parse(e.postData.contents);
    // 保存先のGoogleドライブフォルダIDを指定
    const folderId = "ここにGoogleドライブのフォルダIDを入れる";
    const folder = DriveApp.getFolderById(folderId);
    
    // ファイル名: YYYYMMDD_HHMMSS_店舗名.json
    const now = new Date();
    const timeStr = Utilities.formatDate(now, "JST", "yyyyMMdd_HHmmss");
    const fileName = `receipt_${timeStr}_${data.shop_name}.json`;
    
    folder.createFile(fileName, JSON.stringify(data, null, 2), MimeType.PLAIN_TEXT);
    
    return ContentService.createTextOutput(JSON.stringify({status: "success"}))
      .setMimeType(ContentService.MimeType.JSON);
  } catch (err) {
    return ContentService.createTextOutput(JSON.stringify({status: "error", message: err.toString()}))
      .setMimeType(ContentService.MimeType.JSON);
  }
}
```

この構成を準備することで、スマホ側で「解析・修正・Googleドライブ保存」までが完全に手軽に行えるようになります。

次はこのドライブに溜まったJSONファイルを読み込んで**SQLiteに保存・グラフ表示するPC側（Python）のコード**を作成しましょうか？

---

# you asked

message time: 2026-09-18 21:06:59

Https://ysk-develop.github.io/EVCharge-Advisor/

こちらを参考に作り直してください。

---

# gemini response

参考サイト（EVCharge-Advisor）のデザインコンポーネント、カラーパレット、`localStorage`管理ロジック、セクション構成（`<details>`を活用した設定欄など）に統一して作り直したコードです。

### スマホ用アプリ (`index.html`)

```html
<!doctype html>
<html lang="ja">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover">
<meta name="apple-mobile-web-app-capable" content="yes">
<meta name="apple-mobile-web-app-status-bar-style" content="default">
<meta name="theme-color" content="#111827">
<title>家計簿スキャナー</title>
<style>
:root{--bg:#f5f7fb;--card:#fff;--text:#111827;--muted:#6b7280;--line:#dbe1ea;--accent:#2563eb;--good:#087f5b;--warn:#b45309;--bad:#b91c1c}
*{box-sizing:border-box}
body{margin:0;background:var(--bg);color:var(--text);font-family:-apple-system,BlinkMacSystemFont,"SF Pro Text","Hiragino Sans","Yu Gothic",sans-serif}
main{max-width:760px;margin:auto;padding:20px 16px 48px}
h1{font-size:26px;margin:4px 0 4px}
h2{font-size:18px;margin:0 0 12px}
p{line-height:1.6}
.card{background:var(--card);border:1px solid var(--line);border-radius:18px;padding:16px;margin:12px 0;box-shadow:0 2px 10px rgba(0,0,0,.035)}
.sub{color:var(--muted);font-size:13px;margin-top:0}
button,.filebtn{appearance:none;border:0;border-radius:12px;padding:12px 15px;background:var(--accent);color:#fff;font-size:16px;font-weight:700;cursor:pointer}
button.secondary{background:#e8edf5;color:#1f2937}
button.danger{background:#fee2e2;color:#991b1b}
button.success{background:#ecfdf5;color:#047857;border:2px solid #10b981}
button:disabled{opacity:.5;cursor:not-allowed}
input,select,textarea{width:100%;font:inherit;border:1px solid var(--line);border-radius:10px;padding:10px;background:#fff}
input[type=file]{display:none}
.preview{display:none;width:100%;max-height:360px;object-fit:contain;border-radius:12px;border:1px solid var(--line);margin-top:12px;background:#111}
.actions{display:flex;gap:8px;flex-wrap:wrap;align-items:center}
.status{padding:10px 12px;border-radius:10px;background:#f1f5f9;color:#334155;font-size:14px;white-space:pre-wrap}
.notice{font-size:13px;color:#475569;background:#f8fafc;padding:10px;border-radius:10px}
.row{display:grid;grid-template-columns:1fr 90px 95px 36px;gap:6px;align-items:center;margin:7px 0}
.row.head{font-size:12px;color:var(--muted);font-weight:700}
@media(max-width:560px){
  .row{grid-template-columns:1fr 75px 85px 32px}
  .row input,.row select{padding:8px 6px;font-size:13px}
}
.total-box{display:flex;justify-content:space-between;align-items:center;padding:14px;background:#ecfdf5;border:2px solid #10b981;border-radius:12px;margin-top:12px;font-weight:800}
.total-price{font-size:22px;color:#047857}
.small{font-size:12px;color:var(--muted)}
details summary{cursor:pointer;font-weight:700}
label{display:block;font-weight:700;font-size:14px;margin:10px 0 5px}
hr{border:0;border-top:1px solid var(--line);margin:16px 0}
code{background:#eef2f7;padding:2px 5px;border-radius:5px}
</style>
</head>
<body>
<main>
  <h1>🧾 家計簿スキャナー</h1>
  <p class="sub">レシート写真やメモからAIが品目を抽出し、Googleドライブへ保存します。</p>

  <!-- ① 入力セクション -->
  <section class="card">
    <h2>① 写真またはテキストを選択</h2>
    <div class="actions">
      <label class="filebtn" style="display:inline-block;margin:0;cursor:pointer">
        📷 写真から選択
        <input id="imageInput" type="file" accept="image/*">
      </label>
      <button id="analyzeBtn">AIで解析</button>
      <button id="clearBtn" class="secondary">クリア</button>
    </div>
    <img id="preview" class="preview" alt="選択した画像">
    
    <label for="textMemo" style="margin-top:12px">テキストメモ（画像がない場合）</label>
    <textarea id="textMemo" rows="2" placeholder="例：ドラッグストア、ティッシュ 235円、お茶 100円"></textarea>

    <div id="status" class="status" style="margin-top:12px">画像を選択するか、テキストメモを入力してください。</div>
  </section>

  <!-- ② 解析・確認セクション -->
  <section class="card">
    <h2>② 解析結果・確認修正</h2>
    <div id="detected" class="notice">まだ解析していません。</div>
    
    <div id="editor" style="display:none;margin-top:12px">
      <div style="display:grid;grid-template-columns:1fr 1fr;gap:8px">
        <div>
          <label for="shopName">店舗名</label>
          <input id="shopName" placeholder="店舗名">
        </div>
        <div>
          <label for="receiptDate">日付</label>
          <input id="receiptDate" type="date">
        </div>
      </div>

      <label>明細一覧</label>
      <div class="row head"><div>品目</div><div>金額(円)</div><div>カテゴリ</div><div></div></div>
      <div id="itemList"></div>

      <div class="actions" style="margin-top:8px">
        <button id="addItemBtn" class="secondary" style="width:100%">+ 明細行を追加</button>
      </div>

      <div class="total-box">
        <span>合計金額</span>
        <span id="totalPrice" class="total-price">0 円</span>
      </div>

      <div class="actions" style="margin-top:16px">
        <button id="sendBtn" class="success" style="width:100%">☁ Googleドライブへ保存（送信）</button>
      </div>
    </div>
  </section>

  <!-- ③ 設定セクション -->
  <section class="card">
    <details id="settingsDetails">
      <summary>設定（APIキー・連携設定）</summary>
      
      <label for="apiKey">Gemini API Key</label>
      <input id="apiKey" type="password" autocomplete="off" placeholder="AIzaSy... / 認証キー">
      <div class="actions" style="margin-top:8px">
        <button id="saveKeyBtn">APIキーを保存</button>
        <button id="deleteKeyBtn" class="danger">APIキー削除</button>
      </div>

      <label for="gasUrl">GAS（Google Apps Script）Web App URL</label>
      <input id="gasUrl" placeholder="https://script.google.com/macros/s/.../exec">
      <div class="actions" style="margin-top:8px">
        <button id="saveGasBtn">GAS URLを保存</button>
        <button id="deleteGasBtn" class="danger">URL削除</button>
      </div>

      <label for="model">Geminiモデル</label>
      <input id="model" value="gemini-2.5-flash">

      <p class="small" style="margin-top:12px">※ 設定情報はこの端末の<code>localStorage</code>にのみ保存されます。</p>
    </details>
  </section>

  <!-- ④ アプリについて -->
  <section class="card">
    <details>
      <summary>このアプリについて</summary>
      <p>レシート画像やテキストメモからGemini APIを利用して品目・金額を抽出し、画面上で確認・修正した上でGoogleドライブへ自動同期します。</p>
      <p>保存されたデータはPC側の家計簿アプリ（Python / SQLite）で集計やグラフ表示に利用されます。</p>
    </details>
  </section>
</main>

<script>
const DEFAULT_CATEGORIES = ["食費", "日用品", "交通費", "交際費", "衣服・美容", "趣味・教養", "医療・健康", "水道・光熱費", "その他"];

const $ = id => document.getElementById(id);
let imageData = null;
let imageMime = "image/jpeg";

function escapeHtml(s){
  return String(s || "").replace(/[&<>"']/g, c => ({ "&":"&amp;", "<":"&lt;", ">":"&gt;", '"':"&quot;", "'":"&#39;" }[c]));
}

function fileToDataURL(file){
  return new Promise((resolve, reject) => {
    const r = new FileReader();
    r.onload = () => resolve(r.result);
    r.onerror = reject;
    r.readAsDataURL(file);
  });
}

// 初期設定の読み込み
const savedKey = localStorage.getItem("gemini_api_key");
if(savedKey) $("apiKey").value = savedKey;

const savedGas = localStorage.getItem("gas_script_url");
if(savedGas) $("gasUrl").value = savedGas;

if(!savedKey || !savedGas) $("settingsDetails").open = true;

$("receiptDate").value = new Date().toISOString().split("T")[0];

// 設定イベント
$("saveKeyBtn").onclick = () => {
  const k = $("apiKey").value.trim();
  if(!k) return alert("APIキーを入力してください。");
  localStorage.setItem("gemini_api_key", k);
  alert("APIキーを保存しました。");
};
$("deleteKeyBtn").onclick = () => {
  localStorage.removeItem("gemini_api_key");
  $("apiKey").value = "";
  alert("APIキーを削除しました。");
};

$("saveGasBtn").onclick = () => {
  const g = $("gasUrl").value.trim();
  if(!g) return alert("GAS Web App URLを入力してください。");
  localStorage.setItem("gas_script_url", g);
  alert("GAS URLを保存しました。");
};
$("deleteGasBtn").onclick = () => {
  localStorage.removeItem("gas_script_url");
  $("gasUrl").value = "";
  alert("GAS URLを削除しました。");
};

// ファイル選択・クリア
$("imageInput").onchange = async e => {
  const file = e.target.files?.[0];
  if(!file) return;
  imageMime = file.type || "image/jpeg";
  const dataUrl = await fileToDataURL(file);
  imageData = dataUrl.split(",")[1];
  $("preview").src = dataUrl;
  $("preview").style.display = "block";
  $("status").textContent = `画像を読み込みました。\n${file.name} / ${(file.size/1024/1024).toFixed(2)} MB`;
};

$("clearBtn").onclick = () => {
  imageData = null;
  $("imageInput").value = "";
  $("textMemo").value = "";
  $("preview").style.display = "none";
  $("detected").textContent = "まだ解析していません。";
  $("editor").style.display = "none";
  $("status").textContent = "画像を選択するか、テキストメモを入力してください。";
};

// 明細編集テーブル制御
function renderItems(items = []){
  const container = $("itemList");
  container.innerHTML = "";
  if(!items.length) addItemRow();
  else items.forEach(item => addItemRow(item.name, item.price, item.category));
  calcTotal();
}

function addItemRow(name = "", price = "", category = "食費"){
  const div = document.createElement("div");
  div.className = "row";
  
  const options = DEFAULT_CATEGORIES.map(c => `<option ${c===category?"selected":""}>${c}</option>`).join("");

  div.innerHTML = `
    <input class="i-name" value="${escapeHtml(name)}" placeholder="品目名">
    <input class="i-price" type="number" value="${price}" placeholder="金額">
    <select class="i-cat">${options}</select>
    <button class="danger" style="padding:8px 4px;font-size:12px" onclick="this.parentElement.remove();calcTotal();">✕</button>
  `;
  div.querySelector(".i-price").addEventListener("input", calcTotal);
  $("itemList").appendChild(div);
  calcTotal();
}

$("addItemBtn").onclick = () => addItemRow();

function calcTotal(){
  let total = 0;
  document.querySelectorAll(".i-price").forEach(el => total += Number(el.value) || 0);
  $("totalPrice").textContent = `${total.toLocaleString()} 円`;
}

// AI解析
$("analyzeBtn").onclick = analyze;

async function analyze(){
  const key = localStorage.getItem("gemini_api_key") || $("apiKey").value.trim();
  if(!key){ alert("設定からGemini API Keyを保存してください。"); return; }

  const memo = $("textMemo").value.trim();
  if(!imageData && !memo){ alert("先に写真を選択するかテキストメモを入力してください。"); return; }

  $("status").textContent = "Geminiで解析しています…";
  $("analyzeBtn").disabled = true;

  const prompt = `
レシート画像またはテキストメモから情報を抽出してください。
- shop_name: 店舗名（不明な場合は"不明"）
- date: 日付（YYYY-MM-DD形式。不明なら今日の日付）
- items: 品目リスト（品目名 name, 金額 price, カテゴリ category）

カテゴリ候補: [食費, 日用品, 交通費, 交際費, 衣服・美容, 趣味・教養, 医療・健康, 水道・光熱費, その他]
JSONのみを出力してください。
`;

  const parts = [];
  if(imageData) parts.push({ inline_data: { mime_type: imageMime, data: imageData } });
  if(memo) parts.push({ text: `【入力メモ】\n${memo}` });
  parts.push({ text: prompt });

  const body = {
    contents: [{ parts: parts }],
    generationConfig: {
      responseMimeType: "application/json",
      responseSchema: {
        type: "OBJECT",
        properties: {
          shop_name: { type: "STRING" },
          date: { type: "STRING" },
          items: {
            type: "ARRAY",
            items: {
              type: "OBJECT",
              properties: {
                name: { type: "STRING" },
                price: { type: "NUMBER" },
                category: { type: "STRING" }
              },
              required: ["name", "price", "category"]
            }
          }
        },
        required: ["shop_name", "date", "items"]
      }
    }
  };

  try {
    const model = $("model").value.trim() || "gemini-2.5-flash";
    const url = `https://generativelanguage.googleapis.com/v1beta/models/${encodeURIComponent(model)}:generateContent`;
    const resp = await fetch(url, {
      method: "POST",
      headers: { "x-goog-api-key": key, "Content-Type": "application/json" },
      body: JSON.stringify(body)
    });

    const raw = await resp.text();
    if(!resp.ok) throw new Error(`HTTP ${resp.status}\n${raw}`);
    const data = JSON.parse(raw);
    const text = data?.candidates?.[0]?.content?.parts?.map(p => p.text || "").join("") || "";
    const parsed = JSON.parse(text);

    // 抽出結果の表示
    $("detected").innerHTML = `<b>検出結果:</b> ${escapeHtml(parsed.shop_name)} / ${escapeHtml(parsed.date)} (${parsed.items?.length || 0}件)`;
    $("shopName").value = parsed.shop_name || "";
    if(parsed.date && parsed.date.match(/^\d{4}-\d{2}-\d{2}$/)) $("receiptDate").value = parsed.date;
    renderItems(parsed.items || []);

    $("editor").style.display = "block";
    $("status").textContent = "解析完了。内容を確認・修正してください。";
  } catch(err) {
    console.error(err);
    $("status").textContent = "解析に失敗しました。";
    $("detected").innerHTML = `<span style="color:var(--bad)"><b>エラー:</b> ${escapeHtml(err.message)}</span>`;
  } finally {
    $("analyzeBtn").disabled = false;
  }
}

// ドライブ送信
$("sendBtn").onclick = async () => {
  const gasUrl = localStorage.getItem("gas_script_url") || $("gasUrl").value.trim();
  if(!gasUrl){ alert("設定からGAS Web App URLを保存してください。"); return; }

  const items = [];
  document.querySelectorAll("#itemList .row").forEach(row => {
    const name = row.querySelector(".i-name").value.trim();
    const price = Number(row.querySelector(".i-price").value) || 0;
    const category = row.querySelector(".i-cat").value;
    if(name || price > 0) items.push({ name, price, category });
  });

  let totalAmount = 0;
  items.forEach(i => totalAmount += i.price);

  const payload = {
    timestamp: new Date().toISOString(),
    shop_name: $("shopName").value.trim() || "不明",
    date: $("receiptDate").value,
    total_amount: totalAmount,
    items: items
  };

  $("sendBtn").disabled = true;
  $("sendBtn").textContent = "送信中...";

  try {
    await fetch(gasUrl, {
      method: "POST",
      mode: "no-cors",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify(payload)
    });
    alert("Googleドライブへ保存しました！");
    $("clearBtn").click();
  } catch(err) {
    console.error(err);
    alert(`送信エラー: ${err.message}`);
  } finally {
    $("sendBtn").disabled = false;
    $("sendBtn").textContent = "☁ Googleドライブへ保存（送信）";
  }
};
</script>
</body>
</html>
```

**主な変更点・調整箇所**
* **UIスタイル統一**: 参考元のCSS（変数 `--bg`, `--card`, `--accent`, `.filebtn`, `.notice` など）と全く同じレイアウト構造を適用しました。
* **設定の保持・削除**: APIキーとGAS URLをそれぞれ独立して保存・削除できるUIに調整しました。
* **レスポンシブな明細テーブル**: 狭いスマホ画面でも崩れないグリッド構造（`.row`）に変更し、修正・削除をスムーズに行えるようにしています。